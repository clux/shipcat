defaults:
  imagePrefix: "quay.io/babylonhealth"
  chart: base
  replicaCount: 2
regions:
  dev-uk:
    defaults:
      namespace: dev
      version: develop
    env:
      GLOBAL_EVAR: indeed
  dev-global1:
    defaults:
      namespace: dev
      version: develop
    env:
      GLOBAL_EVAR: yup
teams:
- name: devops
