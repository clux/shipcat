name: default-service
resources:
  limits:
    cpu: 800m
    memory: 1024Mi
  requests:
    cpu: 200m
    memory: 512Mi
replicas:
  min: 2
  max: 5
ports:
- 80:8000
env:
  ELASTICSEARCH_LOGS_PASSWORD: "IN_VAULT"
  LOGGING_CONF: "/config/logging.conf"
  NEWRELIC_CONFIG: "/config/newrelic.ini"
